export interface ArtWorkDetails {
    id: string;
    title: string;
    imageSrc: string;
    principalMaker: string;
    description: string;
}

export interface LoadingModel {
    isLoading?: boolean;
}
